apiVersion: v1
kind: Service
metadata:
  name: hello-flask-service
  namespace: hello-flask
spec:
  type: NodePort
  selector:
    app: hello-flask
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      nodePort: 30080   # choose any free nodePort in 30000-32767

