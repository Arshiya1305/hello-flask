apiVersion: v1
kind: Service
metadata:
  name: hello-flask-service
  namespace: hello-flask
spec:
  type: NodePort
  selector:
    app: hello-flask
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      nodePort: 30080   # Must be between 30000–32767

